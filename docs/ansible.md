# ansible — Sprint file

Single source of truth for in-flight work. PM is the only writer; other roles propose changes via the Open Questions table.

**Sprint cadence:** 7 days, Mon → Sun boundary (per 2026-04-25 sprint-1 review).
**Sprint window:** 2026-04-21 → 2026-05-03 (Sprint 1, extended at sprint-1 review to align onto the new Mon-Sun boundary). Sprint 2 starts Mon 2026-05-04.

---

## Current sprint goal

Complete Task B drift reconciliation: re-baseline `--check --diff` against `community.docker` 5.2.0, schedule and execute the Traefik maintenance window, achieve `changed=0` on a second consecutive apply. **✅ Met 2026-04-27** (end-to-end recap `ok=64 changed=1 skipped=4`; the single `changed` is the accepted cosmetic Traefik-start false-positive). Sprint review on 2026-05-03 will set the Sprint 2 goal — likely candidates per the Carry-over Log: backup role (now unblocked), beginning the remaining-WP-sites migration, or both.

---

## Tasks

### Active sprint (week of 2026-04-21)

- ✅ **B0: Re-baseline `--check --diff` post-`community.docker` 5.2.0 upgrade.** Captured 2026-04-27 09:16 at `.checks/baseline.log`. Recap: `ok=64 changed=4`. The `changed=4` vs expected `changed=7` is fully explained by the timeline — original baseline was 2026-04-24 *before* the `logging` role merged; today's capture reflects the post-merge role count (`ok=64` vs prior `ok=53`). Drift items #4 (B1) and #6 (B3) no longer surface in the recap; #5 (B2) still present.
- ✅ **B1: Investigate drift item #4 — bitsalt-staging `.env` drift.** Confirmed resolved 2026-04-27. `webapp/templates/env.j2` renders only `{% for key, value in item.env_vars.items() %}` — no hardcoded `SITE_DOMAIN=` / `SERVICE_NAME=` lines. `webapp : Deploy .env file` reports `ok` for all 4 webapps in both `.checks/baseline.log` and `.checks/post-b2.log`.
- ✅ **B2: Investigate drift items #5 + new `logging` recreate — two WordPress sites and the `logging` stack recreate on every apply.** A4 should have addressed the WP sites; verify under post-`community.docker` 5.2.0 check-mode fidelity. The `logging : Start logging stack` line was not on the original #1–#6 inventory (role merged mid-sprint); folded into B2 because the failure mode is `docker_compose`-shaped and likely shares root cause. **Resolved 2026-04-27** across both halves: `logging` line fixed via PR #2 (`pull: always` removed against pinned tag); WP recreate resolved via OQ-7 (Option (a) inference held — see `.checks/oq-7-apply-no-op.log` and B6 `--tags wordpress` recap `ok=21 changed=0`).
- ✅ **B3: Investigate drift item #6 — `wp-content` chown reports changed every run.** Confirmed resolved 2026-04-27. `wordpress/tasks/main.yml` has no standalone chown task; the `Create wp-content bind mount directory` task uses `ansible.builtin.file` without `recurse: true`, with an inline comment documenting that `recurse: true` was deliberately removed to avoid cosmetic mode churn. The remaining WP `Start` `changed` line is the OQ-7 compose-build issue, not chown — confirmed not masked.
- ✅ **B4: Schedule Traefik maintenance window.** Resolved 2026-04-27 — windows are open all day today and tomorrow morning if needed; coordination with site owners of `bitsalt.com`, `frujo.net`, `mybusinessmagnet.tech` complete.
- ✅ **B5: Execute Task B step 4 — apply `--tags traefik` in the scheduled window.** Resolved 2026-04-27. Apply reported one `changed` (`traefik : Start Traefik stack`), but `docker ps` on the server shows the Traefik container has been running for several days — i.e., no actual recreate happened. False-positive of the same `docker_compose` family as OQ-7 / the logging line; we accept and live with the cosmetic noise.
- ✅ **B6: Apply remaining roles end-to-end.** Resolved 2026-04-27. All six tagged applies came back `changed=0`: `common` (`ok=10`), `docker` (`ok=13`), four Node.js webapps via `node_site_filter` (each `ok=10 skipped=1`: `bitsalt`, `bitsalt-staging`, `frujo`, `mybizmag`), and the two enabled WordPress sites in one `--tags wordpress` run (`ok=21 skipped=2`: `taotedev`, `seaislandlife`). Logs under `.checks/b6-*.log`. Notably, the WP `Start` tasks reported `ok` (not `changed`) — the OQ-7 `build: .` false-positive did not materialize. WP container uptimes (>2h, matching the operator's earlier site bring-up) confirm no actual recreate.
- ✅ **B7: Verify idempotence.** Resolved 2026-04-27. End-to-end `ansible-playbook playbooks/site.yml --ask-vault-pass` recap: `ok=64 changed=1 skipped=4`. The single `changed` line is the previously-accepted `traefik : Start Traefik stack` cosmetic false-positive (B5). All other roles (common, docker, four webapps, two WP sites, logging) are fully idempotent. **Task B complete; sprint goal met.**

### Sprint 2 (Mon 2026-05-04 → Sun 2026-05-10) — promoted from Carry-over at 2026-05-03 sprint review

- ⬜ **S2-1: OQ-4 retire sweep.** Small opening pass: retire `status.md` and `docs/refactor.md` (content fully absorbed into sprint file + ADRs). Sweep-shaped — also remove any remaining cross-references in `architecture.md`, ADRs, `playbooks/README.md`, etc. Closes OQ-4. ~30-min task.
- ⬜ **S2-2: Backup cron role (FR9 / `add-daily-backups` user story).** Promoted from Carry-over Log; unblocked by Task B closure 2026-04-27. Sprint 2 goal — gates further WordPress site migrations (Carry-over Log: "Migration of remaining ~11 WordPress sites" trigger is "Task B complete + backup role landed"). Per FR9 + the operator-discipline rationale (no manual changes on the managed server; all backups route through ansible). Implementation shape: dedicated `backup` role with cron-driven daily snapshots; runbook authored alongside.

**Sprint 2 capacity note.** Per portfolio dashboard (corrected 2026-05-03), Sprint 2 has only Mon–Wed reduced-capacity (~2–3 hr/morning); Thu 2026-05-07 onward is full capacity. Sprint 2 ansible bucket (6–8 hrs/sprint) is essentially intact — backup role + OQ-4 sweep fit cleanly.

### Phase 2 platform migration tasks (2026-04-24)

- ✅ **M1: Author requirements.md** — BA seeded full requirements (FR1–FR10, NFR1–NFR6, scope, dependencies).
- ✅ **M2: Author user stories** — BA seeded seven stories under `docs/user-stories/`.
- ✅ **M3: Author architecture.md** — Architect seeded full architecture doc.
- ✅ **M4: Author seven ADRs** — Architect produced 0001–0007 covering foundational decisions.
- ✅ **M5: Author CI-deploy interface spec** — Architect produced `docs/interfaces/ci-deploy.md`.
- ✅ **M6: Author runbooks** — DevOps produced six runbooks (`bootstrap`, `add-wordpress-site`, `add-webapp-site`, `wordpress-updates`, `rollback`, `vault-edit`).
- ✅ **M7: Author ops docs** — DevOps produced three ops docs (`ansible.md`, `ci.md`, `users.md`).
- ✅ **M8: Author `.agent-context.md`, sprint file, dashboard row** — PM (this step).
- ✅ **M9: Tech Writer step** — README, getting-started, onboarding produced; further expanded 2026-04-25 with logging coverage.

---

## Open Questions

| # | Raised by | Question | Proposed resolution | Status |
|---|---|---|---|---|
| OQ-1 | Architect (handoff note) | Should the Docker socket proxy and nginx-sidecar design notes be promoted to standalone ADRs? | Defer; revisit in Phase 3 retro. They are external-constraint-driven implementation details, not choices with meaningful alternatives. | **deferred 2026-04-27 (LAND).** Trigger captured in Carry-over Log ("Phase 3 retro"). No action this sprint. |
| OQ-2 | DevOps (handoff note) | Should `wordpress-core-upgrade.md` and `migrate-wordpress.md` runbooks be authored now or just-in-time on first invocation? | Defer to just-in-time. The user stories document the *what*; the runbook is the *how*, best authored against a real run. | **deferred 2026-04-27 (LAND).** Triggers already in Carry-over Log (`wordpress-core-upgrade.md` = first WP core upgrade; `migrate-wordpress.md` = first WP migration to Droplet). |
| OQ-3 | PM | Should `v1/` be retired now or after the remaining ~11 WordPress site migrations? | Retain `v1/` until at least the first three migrated sites have run cleanly under `playbooks/` end-to-end. Revisit at sprint review. | **resolved 2026-04-26** — `v1/` removed (commit `97352f8`); see decisions log. |
| OQ-4 | PM | Should `status.md` and `docs/refactor.md` be retired now that their content is in the sprint file + ADRs? | Retain through this sprint as a cross-reference; retire at sprint review once Task B is complete. | **✅ Resolved 2026-05-03 (Sprint 1 review).** Trigger fired 2026-04-27 with Task B closure. Retire-sweep marked as Sprint 2 first task (sweep across `status.md`, `docs/refactor.md`, plus any orphaned cross-references). Sprint review confirmed the action belongs in Sprint 2 as opening pass rather than landing in the review pass itself. |
| OQ-5 | PM | Coordinate rename of legacy vault key `taotedev_db_password` → `vault_taotedev_db_password` — when? | Defer. Bundle with a future vault-hygiene pass; not blocking any current work. | **deferred 2026-04-27 (LAND).** Trigger captured in Carry-over Log ("future vault-hygiene pass"). No action this sprint. |
| OQ-6 | PM | `docs/TODO.md` (from remote merge) and the sprint file's Open Questions / Carry-over Log are parallel bug-parking surfaces. Pick one as canonical. | Leaning toward keeping `TODO.md` for "investigations surfaced outside the active session" and the sprint file for in-flight sprint work. Will need a clear handoff convention so issues don't sit in both places. | **landed 2026-04-27 (LAND) — convention adopted.** `docs/TODO.md` = bug-parking surface for investigations surfaced *outside* an active agent session (e.g., live-site issues observed by the operator, ad-hoc findings from log review). Sprint file Open Questions = decisions/scope questions raised *during* an agent session that need PM adjudication; sprint file Carry-over Log = work explicitly deferred from a sprint with a return trigger. **Handoff convention:** if a `TODO.md` item gets picked up into an active session, the PM lands it as either an Open Question (if it needs adjudication) or a sprint task / Carry-over entry (if scoped); the corresponding `TODO.md` line is then struck or removed by whichever role pulls it in. Convention captured here; `TODO.md` itself is not PM-owned and will not be edited as part of this LAND. |
| OQ-7 | Developer (B2 diagnosis, 2026-04-27) | The `wordpress : Start WordPress site` task reports `changed` on every `--check --diff` because compose v2 cannot prove the `build: .` stanza would yield an unchanged image without running the build — same family of false positive as the Traefik handler bind-mount-content case. How to resolve? | **ADR 0009 Proposed (PR [#3](https://github.com/bitsalt/ansible/pull/3)) recommends Option (a)** — accept the in-role `build: .` and annotate as a Traefik-handler-shaped carve-out. Architect's revision (after Jeff supplied operator-as-image-owner clarification + non-restart-of-unchanged-sites hard constraint) reaffirmed (a) on tighter grounds: `recreate: auto` + `build: policy` is documentation-inferred to NOT recreate unchanged containers at apply time, and image changes are already vars-file-coordinated. **Option (d)** (split build/apply, pinned tag in compose) wired in as fallback if DevOps verification capture contradicts the inference. Escalation trigger to (d) sharpened to ~5 migrated WP sites. | **resolved 2026-04-27.** `.checks/oq-7-apply-no-op.log` (morning capture) plus the B6 `--tags wordpress` apply (`.checks/b6-wordpress.log`, `ok=21 changed=0`) both confirm WP `Start` reports `ok` at apply time, not `changed`. The `recreate: auto` + `build: policy` inference held; Option (d) fallback not needed. ADR 0009 status can move from Proposed → Accepted at the next Architect touchpoint. |

Other roles append proposals here; PM lands them at the next sprint review or on demand.

---

## Decisions log

New entries at the top.

### 2026-05-03

- **Sprint 1 review (portfolio).** Sprint 1 closed with Task B drift reconciliation goal **MET 2026-04-27** (six days into the sprint window; final apply recap `ok=64 changed=1 skipped=4`, the lone `changed` being the accepted Traefik-start cosmetic false-positive). Open Questions table fully drained at the 2026-04-27 LAND pass. **OQ-4 resolved this review** — retire-sweep promoted to Sprint 2 first task (S2-1) rather than landing in the review pass itself; sweep is broader than `status.md` + `docs/refactor.md` alone and the Sprint 2 opening slot fits it. **Backup cron role (FR9 / `add-daily-backups`) promoted from Carry-over Log to Sprint 2 active task list (S2-2) as Sprint 2 goal** — Task B closure unblocked it per the 2026-04-27 decisions log; backup role precedes any further WordPress site migrations per the Carry-over Log triggers. **Operator-discipline note lifted** — "no manual changes on the ansible-managed server" applied while Task B was in flight; Task B is closed, so the standing rule reverts to normal change-management discipline. **Capacity note (corrected 2026-05-03):** Sprint 2 reduced-capacity span is only Mon–Wed (3 days, not 5), so the standard 6–8 hr ansible bucket is essentially intact. Portfolio-level review entry: see `platform/Projects.md` § Decisions, 2026-05-03 entries.

### 2026-04-27

- **B2 status corrected to ✅.** B2 was left 🟡 in today's earlier LAND pass because LAND drains the Open Questions table, not the task list — the symbol flip needs a deliberate touch. Closing now: the `logging` half resolved via PR #2 (`pull: always` removal), the WP half resolved via OQ-7 (Option (a) inference held; see B6 `--tags wordpress` recap and `.checks/oq-7-apply-no-op.log`). Substance was already captured in the B6, B7, and OQ-7 entries below; this is a bookkeeping correction.
- **PM LAND pass — Open Questions table drained.** Six previously-open OQs adjudicated in one pass; OQ-3 and OQ-7 were already resolved earlier today and were not reopened. **Deferred (3):** OQ-1 (socket-proxy / nginx-sidecar ADR promotion → Phase 3 retro), OQ-2 (`wordpress-core-upgrade.md` / `migrate-wordpress.md` runbooks → just-in-time on first invocation; triggers already in Carry-over Log), OQ-5 (`taotedev_db_password` rename → future vault-hygiene pass). **Trigger fired but action queued (1):** OQ-4 (retire `status.md` and `docs/refactor.md`) — Task B closed today so the proposed-resolution trigger has fired; the retire action runs at the 2026-05-03 sprint review per the original proposal, not in this LAND pass, because the retirement is a sweep across sibling docs rather than a single-file flip and the proposal explicitly named "sprint review" as the moment. **Convention landed (1):** OQ-6 — `docs/TODO.md` is for investigations surfaced *outside* an active agent session; sprint file Open Questions are for in-session decisions/scope; sprint file Carry-over Log is for work deferred with a return trigger. Handoff rule: if a `TODO.md` item enters an active session, PM lands it as an OQ or task and the source line is struck. `TODO.md` itself was not edited (PM does not own that surface). **Escalations (0).** **Out of scope of this LAND:** the portfolio dashboard row update for ansible (Sprint 2 focus selection) and the Carry-over Log re-trigger for the backup role — both belong to the 2026-05-03 sprint review per today's earlier B7 closure entry.
- **B7 closed; Task B complete; sprint goal met.** Second-consecutive end-to-end apply (`ansible-playbook playbooks/site.yml --ask-vault-pass`) recap: `ok=64 changed=1 skipped=4`. The lone `changed` is the cosmetic `traefik : Start Traefik stack` line we accepted at B5; on-server inspection earlier today confirmed the Traefik container has been up for several days. All other roles report idempotent. **Implications for the upcoming sprint review (PM):** (1) operator-discipline note ("no manual changes on the ansible-managed server until Task B closes") can be lifted; (2) the **Backup cron role (FR9 / `add-daily-backups`)** in the Carry-over Log is now unblocked and is the natural next sprint goal candidate; (3) **OQ-4** (retire `status.md` and `docs/refactor.md`) trigger has fired — proposed resolution was "retire at sprint review once Task B is complete"; (4) the portfolio-dashboard row for ansible should flip away from "Task B drift reconciliation" to whatever PM lands as the Sprint 2 focus. None of these are landed in this session — orchestrator does not edit `Projects.md` and does not touch role-owned artifacts beyond the sprint file's PM-allowed surfaces.
- **B6 closed — all remaining roles apply with `changed=0`.** Six tagged applies executed in order: `common` (`ok=10 changed=0`), `docker` (`ok=13 changed=0`), four webapps individually via `--tags webapp -e node_site_filter=<name>` for `bitsalt`, `bitsalt-staging`, `frujo`, `mybizmag` (each `ok=10 changed=0 skipped=1`), and one `--tags wordpress` run covering both enabled WP sites (`taotedev`, `seaislandlife`; `ok=21 changed=0 skipped=2`). Logs in `.checks/b6-*.log`. **OQ-7 fully resolved by this run:** the WP `Start` tasks reported `ok` rather than the expected `changed` false-positive — the `recreate: auto` + `build: policy` inference held end-to-end on top of the morning's `.checks/oq-7-apply-no-op.log` capture. Webapps don't share the false-positive at all (pre-built images; image-hash check is sufficient). The only `changed` line still outstanding across Task B is the cosmetic `traefik : Start Traefik stack` from B5, which we accept.
- **B5 closed — Traefik apply executed; `changed` line is cosmetic.** `ansible-playbook site.yml --tags traefik --ask-vault-pass` reported one `changed` task (`traefik : Start Traefik stack`). On-server `docker ps` shows the Traefik container has been up for several days, confirming no actual recreate occurred. Same false-positive shape as OQ-7 (`build: .`) and the originally-flagged logging-stack line — `docker_compose` cannot prove the stack is unchanged at apply time and reports `changed` on the `Start` task even when the container is untouched. Accepted as live-with cosmetic noise; idempotence assertion in B7 will need to allow for the same line. No `.checks/b5-traefik-apply.log` capture preserved this session — operator-run, outcome verified by direct inspection.
- **OQ-7 routed to Architect; ADR 0009 (Proposed) opened in PR [#3](https://github.com/bitsalt/ansible/pull/3).** Two architect passes ran in sequence. Pass 1 recommended Option (a) on the basis that ADR 0006's app-team-CI boundary doesn't transfer to operator-managed WordPress, and that Option (b) trades visible noise for silent staleness. Pass 2 (after Jeff supplied two clarifying constraints — operator-as-image-owner, and a non-restart-of-unchanged-sites hard constraint) reaffirmed (a) on tighter grounds: `recreate: auto` + `build: policy` (set when `dockerfile_result.changed` is false) is documentation-inferred to NOT recreate unchanged containers at apply time, and image changes are already vars-file-coordinated. **Option (d)** (split build/apply, pinned tag in compose) was introduced and wired in as the fallback if DevOps verification contradicts the inference. Escalation trigger to (d) sharpened from ~20 sites to ~5 migrated WP sites given the ~13 steady-state. ADR Status remains Proposed pending DevOps verification capture at `.checks/oq-7-apply-no-op.log`. Architect log entries at `~/projects/agent-logs/architect.log.md`.
- **B1 and B3 confirmed resolved.** Read-only verification pass against the 2026-04-27 baselines. B1: `webapp/templates/env.j2` renders only loop-driven keys; no hardcoded `SITE_DOMAIN=` / `SERVICE_NAME=`; `Deploy .env file` reports `ok` across all 4 webapps. B3: no standalone chown task in the `wordpress` role; the `Create wp-content bind mount directory` task uses `file` without `recurse: true` (inline comment documents the deliberate removal). The WP `Start` `changed` line is *not* masking a chown task — it's the compose-build issue tracked in OQ-7. Both tasks closed. The remaining gap to `changed=0` is OQ-7 (WP `build: .`) and the B5 Traefik apply (also carries a `pull: always` cleanup riding along, same fix shape as the logging fix).
- **PR #2 / local working-tree reconciliation.** PR [bitsalt/ansible#2](https://github.com/bitsalt/ansible/pull/2) merged on `origin/main` (commit `c05226a`, 10:54 EDT today). Local `main` was un-pulled when Jeff captured `.checks/post-b2.log` at 10:57, which is why the `logging : Start logging stack` line still appeared `changed` post-merge — the working tree on disk was unchanged. After `git pull` + re-run, the logging line is expected to drop. KB lesson `pull-always-with-pinned-tag.md` is accurate and unchanged.
- **B2 partial — `logging` line fix landed; WP recreate blocked on OQ-7.** Developer diagnosed the three changed tasks separately. (1) `logging : Start logging stack` — `pull: always` against pinned tag `timberio/vector:0.47.0-alpine` is a known false-positive pattern under `--check`; second-order case of the existing `check-mode-fidelity-pre-3-10.md` KB lesson. Fix: drop `pull: always`, annotate with block comment cross-referencing the lesson. Branch `developer/ansible/b2-wp-logging-idempotence`; PR [bitsalt/ansible#2](https://github.com/bitsalt/ansible/pull/2). (2) `traefik : Start Traefik stack` — same root cause, **flagged for B5** to absorb (out of B2 scope; gated behind maintenance window). (3) `wordpress : Start WordPress site` (×2) — different root cause: compose v2 can't introspect a `build: .` stanza, same family as the Traefik handler bind-mount-content case from A4. Structural decision; not implemented. Filed as **OQ-7** with three options (accept like Traefik / replace with pre-built image / condition Start on prior task changed-state). Post-fix recap not yet captured — needs vault + droplet access; PR body asks Jeff to capture `.checks/post-b2.log` after merge. Expected: `ok=64 changed=3`. New KB lesson written at `~/projects/agent-knowledge-base/lessons/community.docker/pull-always-with-pinned-tag.md` and committed direct to main.
- **B0 closed against the 09:16 baseline capture.** `.checks/baseline.log` (Jeff, 09:16 today) shows `ok=64 changed=4` against `community.docker` 5.2.0. The expected `changed=7` figure recorded against the 2026-04-24 baseline was pre-`logging` role merge; today's `ok=64` (vs prior `ok=53`) and `changed=4` are internally consistent with the post-merge role count. No fresh re-capture under this session — the 09:16 file is authoritative; re-running would prove only that a different operator typed the command. Baseline file retained.
- **Drift inventory state under 2026-04-27 baseline.** Item #4 (B1): no `Deploy .env file` reports changed → appears resolved; B1 narrows to confirmation. Item #5 (B2): both WP sites still recreate. Item #6 (B3): no standalone chown reports changed → appears resolved or masked by the WP `Start` task; B3 narrows to confirmation. **New finding folded into B2:** `logging : Start logging stack` reports changed; not on the original #1–#6 inventory because the role landed mid-sprint (2026-04-24 remote merge). Folded into B2 rather than spun out because both are `docker_compose`-shaped recreate-on-every-apply behaviors, likely sharing a root cause.
- **B4 resolved — Traefik maintenance windows open today and tomorrow morning.** Site-owner coordination for `bitsalt.com`, `frujo.net`, `mybusinessmagnet.tech` complete; no further scheduling friction. Unblocks B5 once B0–B3 close.

### 2026-04-26

- **`bitsalt-ansible` → `ansible` rename pass landed in this repo (Sprint 1 tail of OQ-PORTFOLIO-12).** Sprint file renamed `docs/bitsalt-ansible.md` → `docs/ansible.md`; title and self-references updated; `.agent-context.md`, `README.md`, `playbooks/README.md`, and the cross-references inside `docs/` (architecture, requirements, interfaces, ops, runbooks, user-stories, getting-started, onboarding) updated to use the new project name. Decisions-log entries from earlier dates are preserved as historical record — they reference `bitsalt-ansible` because that was the project's name at the time. The portfolio dashboard was renamed `BitSalt-Projects.md` → `Projects.md` in the same pass; in-prose references in this file have been updated. Driving OQ at portfolio level: `OQ-PORTFOLIO-12` in `platform/Projects.md`.
- **`v1/` retired (resolves OQ-3 by action).** Jeff removed `archive/` and `v1/` from the working tree (commit `97352f8` on `main`). The OQ-3 proposed resolution had been "retain `v1/` until at least the first three migrated sites have run cleanly under `playbooks/` end-to-end"; that retention rationale turned out not to be load-bearing (nothing in `v1/` was specifically tied to WordPress migrations). OQ-3 closed. Any remaining `v1/` references in `architecture.md`, ADRs, `docs/refactor.md`, `status.md`, and `playbooks/README.md` are out of scope for this rename pass and will be cleaned up in a follow-on sweep.
- **Operator-discipline phrasing updated.** The 2026-04-25 operator-discipline note is preserved verbatim below (historical record); ongoing references in `Projects.md` row blockers and prose now read "no manual changes on the ansible-managed server."

### 2026-04-25

- **Operator-discipline note (durable): no manual changes on the bitsalt-ansible-managed server until Task B closes** (`changed=0` on a second consecutive apply). All infrastructure changes route through ansible. The backup role (FR9 / `add-daily-backups` user story, currently in the Carry-over Log) is gated behind Task B closure for the same reason — adding a new role while drift reconciliation is in flight risks contaminating the baseline. Captured here, in `BitSalt-Projects.md` row blockers, and confirmed at the 2026-04-25 sprint-1 review.
- **Sprint 1 extended to 2026-05-03 and rebased onto Mon-Sun boundary** going forward (Sprint 2 = Mon 2026-05-04 → Sun 2026-05-10), per portfolio-level sprint-1 review. Sprint length stays 7 days; only the boundary moves. See `BitSalt-Projects.md` § Sprint Rhythm.
- **Logging gap-fix arc complete.** The original Phase 2 migration roles ran before the `logging` role merged in mid-flight (see 2026-04-24 entry), so their artifacts had no coverage of it. Today's pass closed those gaps with four backfill artifacts: ADR 0008 `docs/adr/0008-centralized-logging-vector-loki.md` (Architect; see `agent-logs/architect.log.md`), NFR7 added to `docs/requirements.md` (BA; see `agent-logs/business-analyst.log.md`), expanded logging coverage in `docs/getting-started.md` and `docs/onboarding.md` (Tech Writer; see `agent-logs/technical-writer.log.md`), and this PM bookkeeping pass landing M9 ✅ plus updates to `.agent-context.md`, `BitSalt-Projects.md`, and `AGENT-MIGRATION-NOTES.md` (see `agent-logs/project-manager.log.md`). Phase 2 pilot #1 is now genuinely complete on the platform side; legacy `CLAUDE.md` retire awaits Jeff's end-to-end sign-off.

### 2026-04-24

- **`logging` role landed via remote merge** (commit `4689148` and follow-ups in `3a74950`). Cross-cutting Vector → Grafana Cloud Loki shipper at `/opt/logging/`, gated by `logging_enabled` (default false). Architecture, ops/ansible, and onboarding docs updated to reflect the new role. A separate `dev.bitsalt.com` Let's Encrypt rate-limit issue was surfaced via the freshly-deployed Loki stack and is tracked in `docs/TODO.md` (separate from this sprint file pending OQ-6).
- **Phase 2 platform migration of bitsalt-ansible complete.** Full set of platform docs landed: requirements (BA), architecture + 7 ADRs + 1 interface spec (Architect), 6 runbooks + 3 ops docs (DevOps), this sprint file + `.agent-context.md` + dashboard row (PM). README and getting-started forthcoming from Tech Writer.
- **Webapp ownership boundary established** — Ansible owns `.env` for webapp sites; the app repo owns `docker-compose.yml` and the deploy flow. Drives ADR 0006 and `docs/interfaces/ci-deploy.md`. Resolved drift inventory item #3.
- **A4 (`recreate: auto` over `recreate: always`) complete.** Commit `8bba511`. Traefik handler retained at `always` with explicit block-comment rationale (compose v2 cannot detect changes to bind-mounted file content; socket-proxy.conf edits would be missed under `auto`).
- **A3 complete across all 6 roles** (`webapp`, `wordpress`, `laravel`, `traefik`, `common`, `docker`). Each declares `meta/argument_specs.yml` and `defaults/main.yml`. Post-A3 baseline `ok=53 changed=9`. Argument-spec validation refuses to run when required fields are missing.
- **`community.docker` upgraded to 5.2.0** (commit `364d33c`). Pinned in `requirements.yml`. KB lesson at `community.docker/check-mode-fidelity-pre-3-10.md`. Post-upgrade baseline: `ok=53 changed=7`. Dry-run mode warnings gone.
- **A2 (ansible-lint + yamllint + CI hook) complete.** Commit `c4bffe3`. Linters exit 0 against `playbooks/`. CI workflow at `.github/workflows/lint.yml` runs on push/PR. Versions pinned in `requirements.txt`.
- **A1 (`no_log: true` on tasks rendering vault values) complete.** Commit `927b264`. Rule: any task whose module arguments or rendered output include a `vault_*` variable carries `no_log: true`.

### 2026-04-23

- **Ansible coding-standards addendum authored** at `~/projects/coding-standards/coding-standards-ansible.md`. Codifies how the core coding standards (§1–§16) map onto Ansible. Drove Tasks A1–A4.

### 2026-04-06

- **taotedev.com fully live in production.** WordPress loads, plugins up to date, SSL via Let's Encrypt/Traefik. First customer-facing site validated end-to-end on the new infra.

---

## Carry-over Log

Items deferred from earlier work that aren't on the active sprint. Each carries the trigger that should bring it back into a sprint.

- **Backup cron role (FR9 / `add-daily-backups` user story).** *Trigger:* before migrating any additional WordPress sites; takes manual pre-migration backups instead until landed. *Source:* status.md "Ansible / infrastructure."
- **UFW / Fail2ban / unattended-upgrades Ansible roles** for re-convergence after a rebuild. *Trigger:* before the first Droplet rebuild scenario, or as a sprint goal once Task B is complete. *Source:* status.md.
- **Migration of remaining ~11 WordPress sites.** *Trigger:* Task B complete + backup role landed (or manual backup discipline). Pace: one at a time. *Source:* status.md "Remaining sites (migration)."
- **Real Docker Hub image names** substituted into `vars/sites/laravel-site.yml` and `vars/sites/node-site-1.yml`. *Trigger:* Laravel and Node.js apps reach a state where their CI builds and pushes images. *Source:* status.md.
- **Set `enabled: true`** in laravel and nodejs site var files. *Trigger:* paired with the Docker Hub image name task above.
- **node-site-2 vars file** (copy from node-site-1 when ready). *Trigger:* a second Node.js site is identified for deploy.
- **Real domain names + credentials** populated in vault for all sites with placeholder values. *Trigger:* per-site, as part of each migration's pre-checks.
- **Per-app-repo `deploy` SSH key generation** + GitHub Secrets setup. *Trigger:* per-app-repo, when bringing the app's CI/CD online.
- **DO Monitoring agent enabled and alerts configured.** *Trigger:* operator decision; non-blocking.
- **`wordpress-core-upgrade.md` runbook.** *Trigger:* first WP core upgrade across the fleet.
- **`migrate-wordpress.md` runbook.** *Trigger:* first migration of an existing WP site to the Droplet.
- **Retirement of `v1/` directory.** *Trigger:* after the first three migrated sites validate cleanly under `playbooks/`. See OQ-3.
- **Retirement of `status.md` and `docs/refactor.md`.** *Trigger:* end of current sprint, after Task B completes. See OQ-4.
- **Coordinated rename `taotedev_db_password` → `vault_taotedev_db_password`.** *Trigger:* future vault-hygiene pass. See OQ-5.
- **Promote socket-proxy and nginx-sidecar design notes to ADRs (or not).** *Trigger:* Phase 3 retro. See OQ-1.

---

## Sprint reviews

### 2026-05-03 — Sprint 1 review

- **Sprint goal (Task B drift reconciliation): MET 2026-04-27** (six days into the sprint). Final end-to-end recap `ok=64 changed=1 skipped=4`; the single `changed` is the accepted `traefik : Start Traefik stack` cosmetic false-positive (B5).
- **Tasks tally:** 16 ✅ (all of B0–B7 + all of M1–M9), 0 🟡, 0 ⬜, 0 ❌. Plus seven Open Questions adjudicated cleanly (OQ-1 deferred, OQ-2 deferred, OQ-3 ✅, OQ-4 ✅ this review, OQ-5 deferred, OQ-6 ✅ convention adopted, OQ-7 ✅).
- **Sprint 2 charter:** Backup cron role (FR9 / `add-daily-backups`) as Sprint 2 goal; OQ-4 retire sweep as small opening pass. Both promoted from Carry-over to active task list this review (S2-1, S2-2). Backup role gates the WordPress-site migration tail per the Carry-over Log triggers.
- **Cadence:** 7-day cadence held cleanly. Task B closed six days in; no project-internal starvation; the WordPress-migration tail did not leak forward into Sprint 1. **No cadence change proposed** for this project.
- **Carry-over moved:** Backup cron role → Sprint 2 active. OQ-3 (`v1/` retirement) and OQ-7 (WP `build:.`) Carry-over entries can be retired in the Sprint 2 sweep alongside `status.md` / `docs/refactor.md` if convenient (no separate trigger needed — both OQs are closed).
- **Operator-discipline note lifted.** "No manual changes on the ansible-managed server until Task B closes" applied while Task B was in flight; Task B is closed, so normal change-management discipline resumes.
- **Portfolio cross-reference:** `platform/Projects.md` § Decisions, 2026-05-03 entries (Sprint 1 portfolio review umbrella; reduced-capacity window correction; pull-forward shape note; cadence verdict).
