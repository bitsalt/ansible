---
# =============================================================================
# Role: docker
# Installs Docker Engine and the compose plugin from Docker's official repo.
# Adds the ansible and deploy users to the docker group.
# =============================================================================

- name: Install Docker GPG key
  ansible.builtin.apt_key:
    url: https://download.docker.com/linux/ubuntu/gpg
    keyring: /etc/apt/keyrings/docker.gpg
    state: present

- name: Add Docker apt repository
  ansible.builtin.apt_repository:
    repo: >
      deb [arch=amd64 signed-by=/etc/apt/keyrings/docker.gpg]
      https://download.docker.com/linux/ubuntu
      {{ ansible_distribution_release }} stable
    state: present
    filename: docker

- name: Install Docker Engine, compose plugin, and Python SDK
  ansible.builtin.apt:
    name:
      - docker-ce
      - docker-ce-cli
      - containerd.io
      - docker-buildx-plugin
      - docker-compose-plugin
      - python3-docker        # required by community.docker Ansible modules
    state: present
    update_cache: true

- name: Ensure Docker service is enabled and running
  ansible.builtin.systemd:
    name: docker
    enabled: true
    state: started

- name: Add ansible user to docker group
  ansible.builtin.user:
    name: ansible
    groups: docker
    append: true

- name: Add deploy user to docker group
  ansible.builtin.user:
    name: "{{ deploy_user }}"
    groups: docker
    append: true

- name: Authenticate with Docker Hub
  community.docker.docker_login:
    username: "{{ vault_dockerhub_username }}"
    password: "{{ vault_dockerhub_token }}"
    state: present
  no_log: true

- name: Create Docker proxy network
  community.docker.docker_network:
    name: "{{ docker_proxy_network }}"
    state: present
