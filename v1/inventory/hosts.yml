---
all:
  hosts:
    droplet:
      ansible_host: YOUR_DROPLET_IP
      ansible_port: 2222           # match SSH_PORT from bootstrap.sh
      ansible_user: ansible
      ansible_ssh_private_key_file: ~/.ssh/ansible_ed25519
