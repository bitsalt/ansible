# bitsalt-ansible — Agent Context

> **For agents only.** Target: scannable in under a minute (~100 lines / under 3 KB as a sanity check). Pointers and non-obvious invariants only — anything paraphrasing a doc belongs as a link instead.

---

## One-liner

Ansible repo that provisions a single-DO-Droplet hosting environment for BitSalt's portfolio of WordPress / Laravel / Node.js / FastAPI sites behind a shared Traefik proxy.

## Stack

- **Language:** Ansible (YAML), Bash (bootstrap.sh).
- **Framework:** Ansible 2.15+, `community.docker` 5.2.0.
- **Tests:** `ansible-lint` + `yamllint` (no functional tests).
- **Deploy:** This repo *is* the deploy mechanism (`ansible-playbook site.yml --ask-vault-pass`). CI runs lint only.

## Stack addenda to read

In addition to `coding-standards/coding-standards.md`:
- `coding-standards/coding-standards-ansible.md`

## Key file locations

- **Sprint file:** `docs/bitsalt-ansible.md`
- **Architecture:** `docs/architecture.md`, `docs/adr/`, `docs/interfaces/`
- **Requirements / stories:** `docs/requirements.md`, `docs/user-stories/`
- **Runbooks / ops:** `docs/runbooks/`, `docs/ops/`
- **Playbooks:** `playbooks/` (source of truth); `v1/` retained pending validation
- **Bootstrap:** `bootstrap.sh`
- **Entry point:** `playbooks/site.yml`

## Current sprint goal

Complete Task B drift reconciliation: re-baseline `--check --diff` against `community.docker` 5.2.0, schedule and execute the Traefik maintenance window, achieve `changed=0` on a second consecutive apply.

## Active blockers

- Traefik maintenance window unscheduled (3 customer-facing sites in blast radius).
- Backup role (FR9) unimplemented; further migrations need manual pre-migration backups.

## Related projects

- `bitsalt-prod`, `redactus` — both deploy to this Droplet.

## Notes specific to this project

- Legacy vault key `taotedev_db_password` (no `vault_` prefix) is intentional. Rename is a future coordinated task.
- `bootstrap.sh` is the only writer for SSH hardening / UFW / Fail2ban / unattended-upgrades. Re-converging Ansible roles for these are a known gap.
- `v1/` retirement is a sprint-level decision pending full-fleet validation of `playbooks/`.
